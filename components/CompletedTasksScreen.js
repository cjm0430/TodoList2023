import React from 'react';
import { View, Text, StyleSheet, ScrollView } from 'react-native';

const CompletedTasksScreen = () => {
  // 완료된 항목을 표시하는 화면 컴포넌트 구현
};

export default CompletedTasksScreen;
